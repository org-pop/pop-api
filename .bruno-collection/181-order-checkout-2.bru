meta {
  name: 181-order-checkout-2
  type: http
  seq: 181
}
post {
  url: {{baseUrl}}/orders/{{userId_a}}/checkout
  body: none
  auth: none
}
headers {
  Authorization: Bearer {{token_a}}
}
script:post-response {
  if (res.status === 200 || res.status === 201) {
    bru.setVar("order2Id", res.body.id);
  }
}
