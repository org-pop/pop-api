meta {
  name: 041-login-userA
  type: http
  seq: 41
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "sweep.a@example.com",
    "password": "SenhaForte123"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("token_a", res.body.token);
    bru.setVar("userId_a", res.body.userId);
  }
}
