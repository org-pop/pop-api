meta {
  name: 043-login-admin
  type: http
  seq: 43
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "sweep.admin@example.com",
    "password": "SenhaForte123"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("token_admin", res.body.token);
    bru.setVar("userId_admin", res.body.userId);
  }
}
