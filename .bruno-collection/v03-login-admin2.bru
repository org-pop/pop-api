meta {
  name: v03-login-admin2
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "sweep.admin2@example.com",
    "password": "SenhaForte123"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("token_admin", res.body.token);
    bru.setVar("userId_admin", res.body.userId);
  }
}
